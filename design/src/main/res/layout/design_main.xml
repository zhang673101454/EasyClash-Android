<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">
    <data>
        <variable
            name="self"
            type="com.github.kr328.clash.design.MainDesign" />
        <variable
            name="clashRunning"
            type="boolean" />
        <variable
            name="forwarded"
            type="String" />
        <variable
            name="mode"
            type="String" />
        <variable
            name="profileName"
            type="String" />
        <variable
            name="colorClashStarted"
            type="int" />
        <variable
            name="colorClashStopped"
            type="int" />
        <variable
            name="hasProviders"
            type="boolean" />
        <variable
            name="homeTab"
            type="boolean" />
        <variable
            name="urlTesting"
            type="boolean" />
        <variable
            name="profilesEmpty"
            type="boolean" />
        <variable
            name="appVersion"
            type="String" />
        <variable
            name="appAuthor"
            type="String" />

        <import type="android.view.View" />
    </data>

    <androidx.coordinatorlayout.widget.CoordinatorLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="?android:attr/colorBackground"
        android:paddingStart="@{self.surface.insets.start}"
        android:paddingEnd="@{self.surface.insets.end}">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:paddingTop="@{self.surface.insets.top}"
            android:paddingBottom="@{self.surface.insets.bottom}">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="48dp"
                android:gravity="center_vertical"
                android:orientation="horizontal"
                android:paddingHorizontal="16dp">

                <ImageView
                    android:layout_width="28dp"
                    android:layout_height="28dp"
                    android:contentDescription="@string/application_name"
                    android:scaleType="fitCenter"
                    android:src="@drawable/easyclash_logo" />

                <LinearLayout
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="10dp"
                    android:layout_weight="1"
                    android:orientation="vertical">

                    <LinearLayout
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:gravity="center_vertical"
                        android:orientation="horizontal">

                        <TextView
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:text="@string/application_name"
                            android:textColor="?android:attr/textColorPrimary"
                            android:textSize="14sp"
                            android:textStyle="bold" />

                        <View
                            android:layout_width="7dp"
                            android:layout_height="7dp"
                            android:layout_marginStart="8dp"
                            android:background="@{clashRunning ? @drawable/bg_ec_dot_on : @drawable/bg_ec_dot_off}" />
                    </LinearLayout>

                    <TextView
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:ellipsize="end"
                        android:maxLines="1"
                        android:text="@{mode}"
                        android:textColor="?android:attr/textColorSecondary"
                        android:textSize="11sp" />
                </LinearLayout>

                <FrameLayout
                    android:layout_width="40dp"
                    android:layout_height="40dp"
                    android:visibility="@{homeTab ? View.GONE : View.VISIBLE}">

                    <ImageView
                        android:layout_width="40dp"
                        android:layout_height="40dp"
                        android:background="?attr/selectableItemBackgroundBorderless"
                        android:contentDescription="@string/delay_test"
                        android:onClick="@{() -> self.requestUrlTest()}"
                        android:padding="8dp"
                        android:src="@drawable/ic_baseline_sync"
                        android:visibility="@{urlTesting ? View.GONE : View.VISIBLE}" />

                    <ProgressBar
                        android:layout_width="40dp"
                        android:layout_height="40dp"
                        android:padding="8dp"
                        android:visibility="@{urlTesting ? View.VISIBLE : View.GONE}" />
                </FrameLayout>

                <ImageView
                    android:layout_width="40dp"
                    android:layout_height="40dp"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:contentDescription="@string/_new"
                    android:onClick="@{() -> self.requestCreate()}"
                    android:padding="8dp"
                    android:src="@drawable/ic_baseline_add"
                    android:visibility="@{homeTab ? View.VISIBLE : View.GONE}" />
            </LinearLayout>

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="40dp"
                android:layout_marginHorizontal="16dp"
                android:layout_marginTop="4dp"
                android:layout_marginBottom="8dp"
                android:background="@drawable/bg_ec_picker"
                android:orientation="horizontal"
                android:padding="3dp">

                <TextView
                    android:layout_width="0dp"
                    android:layout_height="match_parent"
                    android:layout_weight="1"
                    android:background="@{homeTab ? @drawable/bg_ec_picker_active : null}"
                    android:gravity="center"
                    android:onClick="@{() -> self.requestShowHome()}"
                    android:text="@string/tab_connect"
                    android:textColor="@{homeTab ? @color/color_clash_light : @color/color_text_secondary}"
                    android:textSize="13sp"
                    android:textStyle="bold" />

                <TextView
                    android:layout_width="0dp"
                    android:layout_height="match_parent"
                    android:layout_weight="1"
                    android:background="@{homeTab ? null : @drawable/bg_ec_picker_active}"
                    android:gravity="center"
                    android:onClick="@{() -> self.requestOpenProxy()}"
                    android:text="@string/tab_nodes"
                    android:textColor="@{homeTab ? @color/color_text_secondary : @color/color_clash_light}"
                    android:textSize="13sp"
                    android:textStyle="bold" />
            </LinearLayout>

            <FrameLayout
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1">

                <com.github.kr328.clash.design.view.AppRecyclerView
                    android:id="@+id/profile_list"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:clipToPadding="false"
                    android:paddingBottom="36dp"
                    android:scrollbars="none"
                    android:visibility="@{homeTab &amp;&amp; !profilesEmpty ? View.VISIBLE : View.GONE}" />

                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:gravity="center"
                    android:padding="32dp"
                    android:text="@string/profiles_empty_tips"
                    android:textColor="?android:attr/textColorSecondary"
                    android:textSize="14sp"
                    android:visibility="@{homeTab &amp;&amp; profilesEmpty ? View.VISIBLE : View.GONE}" />

                <FrameLayout
                    android:id="@+id/proxy_host"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:visibility="@{homeTab ? View.GONE : View.VISIBLE}" />
            </FrameLayout>
        </LinearLayout>

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="end|bottom"
            android:alpha="0.45"
            android:gravity="end"
            android:orientation="vertical"
            android:paddingStart="12dp"
            android:paddingTop="12dp"
            android:paddingEnd="12dp"
            android:paddingBottom="@{(float) self.surface.insets.bottom + 12f}">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@{@string/app_meta_author(appAuthor)}"
                android:textColor="@color/color_text_secondary"
                android:textSize="9sp" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@{@string/app_meta_version(appVersion)}"
                android:textColor="@color/color_text_secondary"
                android:textSize="9sp" />
        </LinearLayout>
    </androidx.coordinatorlayout.widget.CoordinatorLayout>
</layout>
